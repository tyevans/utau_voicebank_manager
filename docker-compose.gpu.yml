# GPU overlay for worker container.
# Usage: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
services:
  worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
